import styled from 'styled-components';
import {purpleBackgroundColor, fontSize3} from '../Shared/Styles';

export default styled.select`
  ${purpleBackgroundColor}
  ${fontSize3}
  border: 1px solid;
  margin: 5px;
  height: 25px;
  float: right;
`;